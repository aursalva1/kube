name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Clona todo el repositorio

      - name: List files in the working directory  # Depurar la estructura de archivos
        run: ls -al

      - name: List files in product-service-v1 folder  # Si es necesario verificar subcarpeta
        run: ls -al product-service-v1

      - name: Print current working directory  # Imprime el directorio actual para confirmar el contexto
        run: pwd

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./  # Ajusta la ruta según sea necesario
          file: ./Dockerfile  # Ajusta si está en una subcarpeta (por ejemplo: ./product-service-v1/Dockerfile)
          push: true
          tags: aursalva1/product-service-v1:20240918  # Asegúrate de que el tag es correcto
          no-cache: true  # Desactiva el uso de caché en la construcción

      - name: Log out from Docker Hub
        run: docker logout
